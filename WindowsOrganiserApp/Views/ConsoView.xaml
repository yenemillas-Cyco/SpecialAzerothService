<UserControl x:Class="WindowsOrganiserApp.Views.ConsoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1000">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Georgia"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="Margin" Value="0,10,0,6"/>
        </Style>

        <Style x:Key="BossCheck" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#D4C4A0"/>
            <Setter Property="FontFamily" Value="Georgia"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="GridHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#30D4A017"/>
            <Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="FontFamily" Value="Georgia"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="GridRow" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#D4C4A0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#10FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GridCell" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontFamily" Value="Georgia"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#D4C4A0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border CornerRadius="6" Padding="16"
            Background="{DynamicResource PanelBackground}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT: Boss + conso selection -->
            <Border Grid.Column="0" CornerRadius="6" Padding="12" Margin="0,0,8,0"
                    Background="#18D4A017" BorderBrush="#40806030" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <TextBlock Text="BOSSES NAXXRAMAS"
                                   Style="{StaticResource SectionTitle}" HorizontalAlignment="Center"/>

                        <Border Height="1" Margin="0,4,0,8"
                                Background="#40806030"/>

                        <CheckBox Content="Tout sélectionner"
                                  IsChecked="{Binding SelectAll}"
                                  Style="{StaticResource BossCheck}"
                                  FontWeight="Bold" Foreground="#FFD700" Margin="0,0,0,8"/>

                        <Border Height="1" Margin="0,0,0,6"
                                Background="#20806030"/>

                        <ItemsControl ItemsSource="{Binding Bosses}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}"
                                              Content="{Binding Label}"
                                              Style="{StaticResource BossCheck}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Extra categories -->
                        <ItemsControl ItemsSource="{Binding ExtraCategories}" Margin="0,6,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <Border Height="1" Margin="0,8,0,8"
                                                Background="#40806030"/>
                                        <TextBlock Text="{Binding Name}"
                                                   FontFamily="Georgia" FontSize="15" FontWeight="Bold"
                                                   Foreground="#D4A017" Margin="0,0,0,4"/>
                                        <ItemsControl ItemsSource="{Binding Items}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding IsSelected}"
                                                              Content="{Binding FullName}"
                                                              Style="{StaticResource BossCheck}"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- CENTER: Consumables needed -->
            <Border Grid.Column="1" CornerRadius="6" Padding="12" Margin="4,0,4,0"
                    Background="#18D4A017" BorderBrush="#40806030" BorderThickness="1">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="CONSOMMABLES REQUIS"
                               Style="{StaticResource SectionTitle}" HorizontalAlignment="Center"/>

                    <Border DockPanel.Dock="Top" Height="1" Margin="0,4,0,8"
                            Background="#40806030"/>

                    <DataGrid ItemsSource="{Binding Consumables}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="None"
                              Background="Transparent" BorderThickness="0"
                              RowBackground="Transparent"
                              ColumnHeaderStyle="{StaticResource GridHeader}"
                              RowStyle="{StaticResource GridRow}"
                              CellStyle="{StaticResource GridCell}"
                              CanUserResizeRows="False"
                              SelectionMode="Single"
                              HorizontalScrollBarVisibility="Disabled">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="CODE" Binding="{Binding Code}" Width="70"/>
                            <DataGridTextColumn Header="CONSOMMABLE" Binding="{Binding FullName}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Foreground" Value="#D4C4A0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="QTÉ" Binding="{Binding Quantity}" Width="55">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="#FFD700"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <!-- RIGHT: Raw materials recap -->
            <Border Grid.Column="2" CornerRadius="6" Padding="12" Margin="8,0,0,0"
                    Background="#18D4A017" BorderBrush="#40806030" BorderThickness="1">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="MATÉRIAUX PRIMAIRES"
                               Style="{StaticResource SectionTitle}" HorizontalAlignment="Center"/>

                    <Border DockPanel.Dock="Top" Height="1" Margin="0,4,0,8"
                            Background="#40806030"/>

                    <DataGrid ItemsSource="{Binding Materials}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="None"
                              Background="Transparent" BorderThickness="0"
                              RowBackground="Transparent"
                              ColumnHeaderStyle="{StaticResource GridHeader}"
                              RowStyle="{StaticResource GridRow}"
                              CellStyle="{StaticResource GridCell}"
                              CanUserResizeRows="False"
                              SelectionMode="Single"
                              HorizontalScrollBarVisibility="Disabled">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="MATÉRIAU" Binding="{Binding Name}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Foreground" Value="#D4C4A0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="TOTAL" Binding="{Binding Total}" Width="65">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="#FFD700"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
